input {
   beats {
     type => beats
     port => 5044
   }
}

output {
  stdout {
    codec => rubydebug
  }

  elasticsearch {
        host => "localhost"
        port => "9200"
        protocol => "http"
   }
}
